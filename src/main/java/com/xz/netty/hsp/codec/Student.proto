syntax = "proto3";//版本
option java_outer_classname = "StudentPOJO";//类名，也是文件名
//以message管理数据
//会在StudentPOJO外部类生成一个内部类，他是真正发送的对象
message Student{
  //student类中有一个属性，名字为id，类型为int32（protobuf类型）
  //1表示属性序号，不是值
  int32 id = 1;
  string name = 2;
}